/**
 * Placeholder for background jobs and recurring tasks.
 * This keeps cron/queue orchestration concerns out of API controllers.
 */
export const startScheduler = () => {
  return {
    status: 'not_implemented',
    message: 'Scheduler service module is scaffolded for recurring jobs.',
  };
};
